import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    // Configuration options
    // globals: true,
    // environment: 'jsdom',
    // setupFiles: './vitest.setup.js', // Path to setup file if needed
  },
});